name: Daily Review Generator

on:
  workflow_dispatch:
    inputs:
      target_date:
        description: '生成日报的日期 (格式: YYYY-MM-DD, 留空则生成今天)'
        required: false
        default: ''
        type: string
      days_ago:
        description: '或选择几天前 (0=今天, 1=昨天, 优先使用日期)'
        required: false
        default: '0'
        type: choice
        options:
          - '0'
          - '1'
          - '2'
          - '3'
          - '7'

jobs:
  generate-daily-review:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
          
      - name: Install dependencies
        run: |
          pip install openai
          
      - name: Generate daily review
        id: generate
        env:
          OPENAI_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          TARGET_DATE="${{ github.event.inputs.target_date }}"
          DAYS_AGO="${{ github.event.inputs.days_ago || '0' }}"
          
          # 如果指定了日期，使用日期；否则使用 days_ago
          if [ -n "$TARGET_DATE" ]; then
            python .github/scripts/generate-daily-review.py --date "$TARGET_DATE"
          else
            python .github/scripts/generate-daily-review.py $DAYS_AGO
          fi
          
          # 提取生成的日报信息
          DAILY_FILE=$(ls Reviews/Daily/Daily-*.md 2>/dev/null | tail -1)
          if [ -n "$DAILY_FILE" ]; then
            DATE_STR=$(basename "$DAILY_FILE" | grep -oP '\d{4}-\d{2}-\d{2}')
            echo "date_str=$DATE_STR" >> $GITHUB_OUTPUT
            echo "daily_file=$DAILY_FILE" >> $GITHUB_OUTPUT
            echo "has_review=true" >> $GITHUB_OUTPUT
            
            # 生成北京时间
            GENERATED_TIME=$(TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M')
            echo "generated_time=$GENERATED_TIME" >> $GITHUB_OUTPUT
            
            # 提取主题列表
            TOPICS=$(grep -oP '(?<=topics: \[).*(?=\])' "$DAILY_FILE" || echo "N/A")
            echo "topics=$TOPICS" >> $GITHUB_OUTPUT
          else
            echo "has_review=false" >> $GITHUB_OUTPUT
          fi
          
      - name: Create Pull Request
        if: steps.generate.outputs.has_review == 'true'
        uses: peter-evans/create-pull-request@v5
        with:
          branch: daily-review-${{ steps.generate.outputs.date_str }}
          delete-branch: true
          
          commit-message: |
            docs: add daily review ${{ steps.generate.outputs.date_str }}
            
            Auto-generated by DeepSeek AI
            
          title: '📝 Daily Review ${{ steps.generate.outputs.date_str }}'
          
          body: |
            ## 📊 今日学习日报
            
            本 PR 由 AI 自动生成，包含以下内容：
            
            - 📅 **日报日期**: ${{ steps.generate.outputs.date_str }}
            - 📄 **日报文件**: `${{ steps.generate.outputs.daily_file }}`
            - 📚 **学习主题**: ${{ steps.generate.outputs.topics }}
            - 🤖 **生成模型**: DeepSeek Chat
            - ⏰ **生成时间**: ${{ steps.generate.outputs.generated_time }} (UTC+8)
            
            ---
            
            ## ✅ 合并前检查清单
            
            请在合并前确认：
            
            - [ ] 日报内容准确无误
            - [ ] 格式排版正常
            - [ ] 专业术语使用正确
            - [ ] 没有敏感信息
            
            ---
            
            ## 📝 如何修改
            
            如果需要修改内容：
            
            1. 在本 PR 的 **Files changed** 标签页中点击文件
            2. 点击右上角的 **⋯** 按钮，选择 **Edit file**
            3. 修改后点击 **Commit changes**
            4. 修改会自动添加到本 PR
            
            ---
            
            ## 🔀 合并方式
            
            - **Squash and merge** (推荐): 合并为一个 commit
            - **Merge commit**: 保留完整历史
            
            满意后点击 **Merge pull request** 即可！
            
          labels: |
            documentation
            ai-generated
            daily-review
